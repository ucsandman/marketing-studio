{
  "name": "capture-feeder",
  "description": "Playwright capture feeder for Marketing Studio product demos.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@playwright/test": "^1.50.0"
  }
}
